standardizedKddcupExtended <- read.delim("~/Documenti/masl-exam/Relazione/code/standardizedKddcupExtended")
View(standardizedKddcupExtended)
ibrary(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
standardizedComponentsKddcup[-c(19)]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
printcp(fit)
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
standardizedComponentsKddcup[-c(19)]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
standardizedComponentsKddcup <- read.delim("~/Documenti/masl-exam/Relazione/code/standardizedComponentsKddcup")
View(standardizedComponentsKddcup)
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
standardizedComponentsKddcup[-c(19)]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
standardizedComponentsKddcup[-c(19),]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
standardizedComponentsKddcup[-c(19),]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup[-c(19),], control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcup, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
standardizedKddcup <- read.delim("~/Documenti/masl-exam/Relazione/code/standardizedKddcup")
View(standardizedKddcup)
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcup, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
t_pred <- predict(fit, standardizedKddcup, type="class")
standardizedComponentsKddcup$dst_host_srv_diff_host_rate.1 <- NULL
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcup, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedComponentsKddcup$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
fit <- rpart(behaviour ~ .,
data=standardizedComponentsKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedKddcupExtended$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
library(rpart)
library(rpart.plot)
#temp[-c(1),]
#temp1[-c(1),]
fit <- rpart(behaviour ~ .,
data=standardizedKddcup, control=rpart.control(cp=0.0025), method="class")
#plot(fit)
#text(fit, use.n=TRUE, all=TRUE)
plot
rpart.plot(fit, # middle graph
type=4,
extra=101,
box.palette="GnBu",
branch.lty=3,
shadow.col="gray",
nn=TRUE
)
t_pred <- predict(fit, standardizedKddcupExtended, type="class")
confMat <- table(standardizedKddcupExtended$behaviour,t_pred)
accuracy <- sum(diag(confMat))/sum(confMat)
accuracy
